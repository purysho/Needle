name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release tag (for example v1.0.0)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

env:
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}

jobs:
  windows-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Build executable
        shell: powershell
        run: .\build-windows.ps1
      - name: Smoke check executable
        shell: powershell
        run: |
          if (!(Test-Path 'dist\Needle.exe')) { throw 'Needle.exe was not built' }
          if ((Get-Item 'dist\Needle.exe').Length -le 0) { throw 'Needle.exe is empty' }
      - name: Create checksum
        shell: powershell
        run: |
          $hash = (Get-FileHash 'dist\Needle.exe' -Algorithm SHA256).Hash.ToLower()
          "$hash  Needle.exe" | Out-File -Encoding ascii 'dist\Needle.exe.sha256'
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: |
            dist/Needle.exe
            dist/Needle.exe.sha256
